// Reserved for the typed client (mirrors `paykit/client`). Empty for v1 — the
// HTTP API surface is intentionally tiny (orders + shipping resolve), and most
// consumers will call the server methods directly.
export type {} from "../types/instance";
